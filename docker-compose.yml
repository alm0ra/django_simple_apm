version: "3.3"
services:
  admin_panel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: django_simple_apm
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - type: bind
        source: .
        target: /code

networks:
  app-network:
    external: true
